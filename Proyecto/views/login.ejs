<!DOCTYPE html>
<html>
<head>
    <title>Iniciar Sesión</title>
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background: linear-gradient(135deg, #4a6cf7 0%, #1e1e2f 35%, #111827 100%);
            color: #e5e5e5;
            min-height: 100vh;
            display: flex;
            align-items: center;
        }

        .card {
            background: #2b2b40;
            border-radius: 18px;
            color: #e5e5e5;
            box-shadow: 0 18px 35px rgba(0,0,0,0.45);
        }

        label {
            color: #ccc;
        }

        .btn-primary {
            background-color: #4a6cf7;
            border: none;
            font-weight: 600;
        }

        .btn-primary:hover {
            background-color: #3958d7;
        }

        .btn-outline-light {
            border-color: #aaa;
            color: #fff;
        }

        .btn-outline-light:hover {
            background-color: #fff;
            color: #000;
        }

        .app-title {
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 0.12em;
            color: #9ca3ff;
            text-align: center;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-4">
                <div class="card p-4 shadow">
                    <div class="app-title mb-1">Sistema de Registros</div>
                    <h3 class="text-center mb-3">Iniciar Sesión</h3>

                    <% if (error) { %>
                        <div class="alert alert-danger py-2"><%= error %></div>
                    <% } %>

                    <form action="/login" method="POST">

                        <label>Usuario</label>
                        <input class="form-control mb-3" name="usuario" placeholder="Usuario" required>

                        <label>Contraseña</label>
                        <input type="password" class="form-control mb-3" name="password" placeholder="Contraseña" required>

                        <button class="btn btn-primary w-100">Entrar</button>

                        <a href="/adduser" class="btn btn-outline-light w-100 mt-2">Crear usuario</a>

                    </form>

                </div>
            </div>
        </div>
    </div>
</body>
</html>
